

```
     Instalar dependencias
        yarn

     Ejecutar codigo
        yarn dev

    en pruebas con postan, crear usaurio en ruta /api/login
    utilizar el token de logueo para probar endpoints,
    myHeaders.append("x-token", "{aqui va el token cuando te logueas}");

```